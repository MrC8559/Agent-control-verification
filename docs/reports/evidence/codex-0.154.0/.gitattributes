# Preserve captured artifact bytes and their SHA-256 values on every platform.
# Captured Windows CRLF endings are intentional, not trailing whitespace.
* -text whitespace=trailing-space,space-before-tab,cr-at-eol
